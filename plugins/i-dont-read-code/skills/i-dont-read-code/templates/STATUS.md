# Where we are

*Last updated: YYYY-MM-DD*

Written for the person who owns this, not for a developer. Keep it in the project root so they can
open it without asking. Update it at feature completion, at session end, and whenever asked —
not every turn.

Dates are `YYYY-MM-DD` because a script reads them to surface things that have gone stale.

---

## Working, and someone has actually used it

Only items with a `(I watched this work: …)` tag, or that the owner has confirmed themselves.
Nothing gets promoted into this section on its own.

- `YYYY-MM-DD` — <what it does, as they'd notice it>

## Still unproven

Everything that is `(Machine-checked only …)` or `(Not run …)`. This is the outstanding-risk list —
the single most useful section, because it is the one that otherwise lives only in chat scrollback
and quietly disappears.

An item leaves this section **only when the owner says they saw it work.** Not when the code looks
right, not when tests pass, not because time passed.

- `YYYY-MM-DD` — <what was built> — to confirm it: <the one thing they could check, in their world>

## Waiting on you

Open handoffs. Each one keeps the date it was asked, so an item that has sat here 7+ days gets
surfaced once, unprompted, rather than dying quietly.

- `YYYY-MM-DD` — <what they need to do> — <why only they can do it>

## Since last time

For someone coming back after four days. Two or three lines, most recent first. Trim anything older
than the last couple of sessions; this is a landing pad, not a log.

- `YYYY-MM-DD` — <what changed>

## Decisions already made

So the same fork is never asked twice, and so the reasoning survives being forgotten.

- `YYYY-MM-DD` — chose <X> over <Y>, because <the reason in their currency: money, time, risk, what
  people experience>
